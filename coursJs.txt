/* ============================================
   CYBERPUNK DARK MODE THEME - TP4 LIENS EN HTML
   ============================================ */

@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Space+Mono:wght@400;700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Space Mono', monospace;
    line-height: 1.6;
    color: #00ff88;
    background: linear-gradient(135deg, #0a0e27 0%, #1a1a3e 50%, #0f0f1e 100%);
    background-attachment: fixed;
    position: relative;
    overflow-x: hidden;
}

/* Effet de grille en arrière-plan */
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(0deg, transparent 24%, rgba(0, 255, 136, 0.05) 25%, rgba(0, 255, 136, 0.05) 26%, transparent 27%, transparent 74%, rgba(0, 255, 136, 0.05) 75%, rgba(0, 255, 136, 0.05) 76%, transparent 77%, transparent),
        linear-gradient(90deg, transparent 24%, rgba(0, 255, 136, 0.05) 25%, rgba(0, 255, 136, 0.05) 26%, transparent 27%, transparent 74%, rgba(0, 255, 136, 0.05) 75%, rgba(0, 255, 136, 0.05) 76%, transparent 77%, transparent);
    background-size: 50px 50px;
    pointer-events: none;
    z-index: 1;
}

main, header, nav, footer {
    position: relative;
    z-index: 2;
}

/* ============================================
   HEADER
   ============================================ */

header {
    background: linear-gradient(90deg, #0a0e27 0%, #1a0033 50%, #0a0e27 100%);
    border-bottom: 3px solid #00ff88;
    border-top: 3px solid #ff00ff;
    padding: 2rem;
    text-align: center;
    box-shadow: 0 0 30px rgba(0, 255, 136, 0.3), inset 0 0 20px rgba(255, 0, 255, 0.1);
    position: relative;
    overflow: hidden;
}

header::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 255, 136, 0.1), transparent);
    animation: scan 8s linear infinite;
}

@keyframes scan {
    0% { left: -100%; }
    100% { left: 100%; }
}

header h1 {
    font-family: 'Orbitron', sans-serif;
    font-size: 2.8rem;
    font-weight: 900;
    color: #00ff88;
    text-shadow: 0 0 10px #00ff88, 0 0 20px #ff00ff, 0 0 30px #00ffff;
    margin: 0;
    letter-spacing: 3px;
    position: relative;
    z-index: 3;
}

/* ============================================
   TITRES
   ============================================ */

h1 {
    font-family: 'Orbitron', sans-serif;
    color: #00ff88;
    text-shadow: 0 0 10px #00ff88, 0 0 20px #ff00ff;
    font-weight: 900;
    letter-spacing: 2px;
}

h2 {
    font-family: 'Orbitron', sans-serif;
    color: #00ffff;
    font-size: 2rem;
    border-bottom: 2px solid #ff00ff;
    border-top: 1px solid #00ff88;
    padding: 1rem 0;
    margin: 2rem 0 1rem 0;
    text-shadow: 0 0 10px #00ffff, 0 0 20px #ff00ff;
    letter-spacing: 1px;
}

h3 {
    font-family: 'Orbitron', sans-serif;
    color: #ff00ff;
    font-size: 1.5rem;
    margin: 1.5rem 0 0.5rem 0;
    text-shadow: 0 0 8px #ff00ff;
    letter-spacing: 1px;
}

/* ============================================
   PARAGRAPHES ET TEXTE
   ============================================ */

p {
    margin-bottom: 1rem;
    text-align: justify;
    line-height: 1.8;
    color: #00ff88;
}

/* ============================================
   CODE
   ============================================ */

code {
    background-color: rgba(0, 0, 0, 0.8);
    color: #00ff88;
    padding: 0.2rem 0.5rem;
    border-radius: 3px;
    border: 1px solid #00ff88;
    font-family: 'Space Mono', monospace;
    font-size: 0.9rem;
    box-shadow: 0 0 10px rgba(0, 255, 136, 0.3);
}

pre {
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.9) 0%, rgba(10, 14, 39, 0.9) 100%);
    color: #00ff88;
    padding: 1.5rem;
    border-radius: 5px;
    border: 2px solid #00ff88;
    border-left: 4px solid #ff00ff;
    overflow-x: auto;
    margin: 1rem 0;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2), inset 0 0 20px rgba(0, 255, 136, 0.05);
    position: relative;
}

pre::before {
    content: '> ';
    color: #ff00ff;
    font-weight: bold;
}

pre code {
    background-color: transparent;
    color: #00ff88;
    padding: 0;
    border: none;
    box-shadow: none;
}

/* ============================================
   MAIN CONTENT
   ============================================ */

main {
    max-width: 1200px;
    margin: 2rem auto;
    padding: 0 1rem;
}

/* ============================================
   GRILLE DE COURS
   ============================================ */

.cours-grid {
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;
    gap: 2rem;
    margin: 2rem 0;
}

.cours-item {
    background: linear-gradient(135deg, rgba(26, 26, 62, 0.8) 0%, rgba(15, 15, 30, 0.8) 100%);
    border: 2px solid #00ff88;
    border-radius: 10px;
    padding: 2rem;
    text-align: center;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2), inset 0 0 10px rgba(0, 255, 136, 0.05);
    transition: all 0.3s ease;
    width: 250px;
    position: relative;
    overflow: hidden;
}

.cours-item::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(45deg, transparent, rgba(255, 0, 255, 0.1), transparent);
    transform: rotate(45deg);
    animation: shimmer 3s infinite;
}

@keyframes shimmer {
    0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
    100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
}

.cours-item:hover {
    border-color: #ff00ff;
    box-shadow: 0 0 30px rgba(255, 0, 255, 0.5), inset 0 0 20px rgba(255, 0, 255, 0.1);
    transform: translateY(-10px) scale(1.05);
}

.cours-item a {
    text-decoration: none;
    color: inherit;
    position: relative;
    z-index: 2;
}

.cours-item .logo {
    width: 120px;
    height: 120px;
    object-fit: contain;
    margin-bottom: 1rem;
    filter: drop-shadow(0 0 10px rgba(0, 255, 136, 0.5));
    transition: filter 0.3s ease;
}

.cours-item:hover .logo {
    filter: drop-shadow(0 0 20px rgba(255, 0, 255, 0.8));
}

.cours-item h3 {
    color: #00ff88;
    margin-top: 1rem;
    text-shadow: 0 0 10px #00ff88;
}

.cours-item:hover h3 {
    color: #ff00ff;
    text-shadow: 0 0 15px #ff00ff;
}

/* ============================================
   NAVIGATION
   ============================================ */

nav {
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.8) 0%, rgba(26, 0, 51, 0.8) 100%);
    padding: 1rem;
    margin: 1rem 0;
    border-left: 4px solid #00ff88;
    border-right: 4px solid #ff00ff;
    border-radius: 5px;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
}

nav a {
    color: #00ff88;
    text-decoration: none;
    margin-right: 1.5rem;
    font-weight: 700;
    transition: all 0.3s ease;
    text-shadow: 0 0 5px #00ff88;
    position: relative;
}

nav a::before {
    content: '▶ ';
    color: #ff00ff;
    margin-right: 0.5rem;
    opacity: 0;
    transition: opacity 0.3s ease;
}

nav a:hover {
    color: #ff00ff;
    text-shadow: 0 0 15px #ff00ff;
    padding-left: 10px;
}

nav a:hover::before {
    opacity: 1;
}

/* ============================================
   LISTES
   ============================================ */

ul, ol {
    margin-left: 2rem;
    margin-bottom: 1rem;
}

li {
    margin-bottom: 0.8rem;
    color: #00ff88;
}

a {
    color: #00ffff;
    text-decoration: none;
    transition: all 0.3s ease;
    text-shadow: 0 0 5px #00ffff;
    position: relative;
}

a::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #00ffff, #ff00ff);
    transition: width 0.3s ease;
}

a:hover {
    color: #ff00ff;
    text-shadow: 0 0 15px #ff00ff;
}

a:hover::after {
    width: 100%;
}

/* ============================================
   SOMMAIRE INTERNE
   ============================================ */

.sommaire-interne {
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.8) 0%, rgba(26, 0, 51, 0.6) 100%);
    padding: 1.5rem;
    border-left: 4px solid #00ff88;
    border-top: 2px solid #ff00ff;
    border-bottom: 2px solid #ff00ff;
    margin: 2rem 0;
    border-radius: 5px;
    box-shadow: 0 0 25px rgba(0, 255, 136, 0.2), inset 0 0 15px rgba(255, 0, 255, 0.05);
}

.sommaire-interne h2 {
    margin-top: 0;
    color: #00ffff;
}

.sommaire-interne ul {
    list-style-type: none;
    margin-left: 0;
}

.sommaire-interne li {
    margin-bottom: 0.8rem;
}

.sommaire-interne a {
    color: #00ff88;
    font-weight: 500;
    text-shadow: 0 0 5px #00ff88;
}

.sommaire-interne a:hover {
    color: #ff00ff;
    text-shadow: 0 0 15px #ff00ff;
}

/* ============================================
   RETOUR AU SOMMAIRE
   ============================================ */

.back-to-top {
    display: inline-block;
    margin: 1rem 0;
    padding: 0.7rem 1.5rem;
    background: linear-gradient(90deg, rgba(0, 255, 136, 0.1) 0%, rgba(255, 0, 255, 0.1) 100%);
    color: #00ff88;
    border: 2px solid #00ff88;
    border-radius: 5px;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    font-weight: 700;
    text-shadow: 0 0 5px #00ff88;
    cursor: pointer;
}

.back-to-top:hover {
    background: linear-gradient(90deg, rgba(0, 255, 136, 0.3) 0%, rgba(255, 0, 255, 0.3) 100%);
    border-color: #ff00ff;
    color: #ff00ff;
    box-shadow: 0 0 20px rgba(255, 0, 255, 0.5);
    text-shadow: 0 0 15px #ff00ff;
}

/* ============================================
   SECTION DE RESSOURCES
   ============================================ */

.ressources {
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.9) 0%, rgba(26, 0, 51, 0.8) 100%);
    padding: 2rem;
    border-radius: 10px;
    margin: 2rem 0;
    border: 2px solid #ff00ff;
    box-shadow: 0 0 30px rgba(255, 0, 255, 0.3), inset 0 0 20px rgba(255, 0, 255, 0.05);
}

.ressources h3 {
    color: #00ffff;
    border-bottom: 2px solid #00ff88;
    padding-bottom: 0.5rem;
    text-shadow: 0 0 10px #00ffff;
}

.ressources a {
    display: inline-block;
    margin-right: 1.5rem;
    margin-bottom: 1rem;
    padding: 0.8rem 1.5rem;
    background: linear-gradient(90deg, #00ff88 0%, #00ffff 100%);
    color: #000;
    border-radius: 5px;
    transition: all 0.3s ease;
    font-weight: 700;
    box-shadow: 0 0 15px rgba(0, 255, 136, 0.5);
    text-shadow: none;
}

.ressources a:hover {
    background: linear-gradient(90deg, #ff00ff 0%, #00ffff 100%);
    box-shadow: 0 0 25px rgba(255, 0, 255, 0.8);
    transform: translateY(-3px);
}

.ressources a::after {
    display: none;
}

/* ============================================
   FOOTER
   ============================================ */

footer {
    background: linear-gradient(90deg, #0a0e27 0%, #1a0033 50%, #0a0e27 100%);
    color: #00ff88;
    text-align: center;
    padding: 2rem;
    margin-top: 3rem;
    font-size: 0.9rem;
    border-top: 3px solid #00ff88;
    border-bottom: 3px solid #ff00ff;
    box-shadow: 0 0 30px rgba(0, 255, 136, 0.2), inset 0 0 20px rgba(255, 0, 255, 0.1);
    text-shadow: 0 0 10px #00ff88;
}

/* ============================================
   TABLES
   ============================================ */

table {
    border-collapse: collapse;
    width: 100%;
    margin: 1rem 0;
    background: rgba(0, 0, 0, 0.8);
    border: 2px solid #00ff88;
    box-shadow: 0 0 20px rgba(0, 255, 136, 0.2);
}

table tr {
    border: 1px solid #00ff88;
}

table td {
    border: 1px solid #00ff88;
    padding: 0.8rem;
    color: #00ff88;
    text-align: center;
}

table tr:hover {
    background-color: rgba(0, 255, 136, 0.1);
}

/* ============================================
   RESPONSIVE
   ============================================ */

@media (max-width: 768px) {
    header h1 {
        font-size: 1.8rem;
    }

    h2 {
        font-size: 1.5rem;
    }

    .cours-grid {
        flex-direction: column;
        align-items: center;
    }

    .cours-item {
        width: 100%;
        max-width: 300px;
    }

    nav a {
        display: block;
        margin-bottom: 0.5rem;
    }

    pre {
        font-size: 0.8rem;
        padding: 1rem;
    }
}

/* ============================================
   ANIMATIONS SUPPLÉMENTAIRES
   ============================================ */

@keyframes glow {
    0%, 100% { text-shadow: 0 0 10px #00ff88; }
    50% { text-shadow: 0 0 20px #00ff88, 0 0 30px #ff00ff; }
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
}

h1 {
    animation: glow 3s ease-in-out infinite;
}
